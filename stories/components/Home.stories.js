import React from "react";
import App from "components/App";
import Home from "components/Home";

export const Basic = () => (
  <App>
    <Home />
  </App>
);

export default {
  title: "Home",
  component: Home,
};
